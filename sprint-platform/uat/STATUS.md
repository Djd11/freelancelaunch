# UAT STATUS BOARD
Format: `[<bot>] <timestamp> <event>`

[orchestrator] 2026-09-07 22:26 UAT kickoff. Target: https://freelancelaunch.onrender.com. BRIEF.md written. Handing to architect for UAT_PLAN.md.
[orchestrator] 2026-09-07 22:52 UAT_PLAN.md written (9 areas, risk-ordered, per-area rubric). Tester next: waiting on tmux install for interactive session.
[tester] 2026-09-07 23:05 START area 1 (auth): login render, wrong-password, seeded login 302, logout, anon redirect, CSRF. Non-headless Chrome DISPLAY=:0, screenshots → uat/screenshots/, report → uat/reports/auth.md.
[orchestrator] 2026-09-07 23:50 Area 1 (auth) report finalized from tester transcript (hit 60-turn cap before file writes): FAIL — BUG-1 password never validated. Report: uat/reports/auth.md, 6 screenshots.
[builder] 2026-09-09 11:28 BUG-1/2/3 fix: login now validates passwords via Supabase auth (sign_in_with_password), generic error copy, /login redirect, password field + required attrs. auth-hardening.feature 7/7 green. Full-suite regression in progress.
[test] 2026-09-10 10:20 verify-auth-fix: CONFIRMED CORRECT — BUG-1/2/3 fixed (auth-hardening 7/7, 8/8 review checks PASS, session UUID = real auth.users id); full suite: 58 fails = baseline set, 0 auth regressions (6 new non-passing = transient DNS/DB-interference window 10:02-10:12, evidence in uat/reports/verify-auth-fix.md). Report: uat/reports/verify-auth-fix.md
[test] 2026-09-11T09:18Z verify-auth-fix: auth-hardening 7/7 green; full-suite pending — VERDICT CONFIRMED CORRECT so far

[test] 2026-09-11T09:18Z verify-auth-fix: auth-hardening 7/7 green — SPEC GREEN; full-suite regression still PENDING (Step 3 not yet run)
[uat-validator] 2026-09-15 18:08 OTP/social UAT plan written: uat/UAT_PLAN_OTP_SOCIAL.md (12 areas A1–A12 + P1–P5 preconditions + G1–G5 gaps). Tooling pre-verified: playwright 1.62 driving system Chrome (channel=chrome, DISPLAY=:0) launches; local app boots vs live Supabase (GET /auth/login 200, GET / 200, port 5000 released again). OTP-without-mailbox method settled: admin generate_link -> properties.email_otp, fed to POST /auth/otp/verify. Execution awaits backend commit (P1) + captain answers on live-test-user writes and dashboard ownership.
[uat-validator] 2026-09-15 18:35 Captain rulings absorbed into the plan (§0a binding constraints, live-send schedule, A2/A6/A8/A9b transport labels, G6–G8 gaps). Built enforcement harness uat/scripts/otp_uat_harness.py: FakeAuth (records sign_in_with_otp/verify_otp/exchange args), seam-resolving make_app, ledger-capped live helpers (8 users, 2 sends/hour, uat-otp-* only) — self-checked, zero live calls made. Pre-flight P0 added for the verify_otp type-literal risk (EmailOtpType includes both "email" and "magiclink" and the TypedDict does not validate, so only GoTrue decides). CORRECTION to my own earlier note: I first wrote behave baseline numbers second-hand (442/90min/43) then verified them against uat/reports/behave-full-run1.log — the real figures are 210 scenarios, 145 pass / 58 fail / 7 error, 30m38s; G6 rewritten from primary evidence. HOLDING execution: working-tree routes/auth.py is still the pre-change version (collision auto-login present at l.81), so P1 is false; waiting for the t6-final build.
[uat-validator] 2026-09-15 18:44 Second self-correction, again caught at the source before it could reach a report: live_db_adapter.cleanup_scenario (l.381) deletes only scenario-TRACKED rows (sprints by tracked id + the four fixture users' user_profiles), NOT "all rows for the seeded users" as I first wrote. Net effect is favourable — my uat-otp-* identities are invisible to behave, so provisioning evidence is cross-talk-free; but behave never cleans them up either, so the report's purge list is load-bearing. Also confirmed against the captain's 2-send budget: A6's admin-address case is fake-transport evidence (both live slots go to A2's enumeration pair). Gaps G1–G8 final; harness self-check passes; ledger untouched, zero live calls. Still holding for t6.
[uat-validator] 2026-09-16 11:15 **P1 correction — the code IS here**: T1–T5 landed in this worktree as real commits (T1 7f36903 → T2 6a8f400 → T3 493493a → T5 a70be61 → T5-addendum 0c7171e), so my 18:44 "P1 false" line is superseded (it described the lost /tmp worktree). Driver built + self-tested write-free: `uat/scripts/run_t7_uat.py` — 11 steps, one process per step (fake/patch isolation), ordered by SMTP cost; `--report` renders the committed skeleton from evidence JSON; `--selftest` output is quarantined to `otp-social-login.SELFTEST-preview.md` under a NOT-UAT-EVIDENCE banner. On 0c7171e: **70 assertions, 0 fail, 3 named gaps** (25 LIVE / 23 FAKE-TRANSPORT / 22 STATIC), 1m44s, zero live writes, ledger untouched, port 5000 free. Three false-result traps caught and fixed while self-testing: (1) A2 would have "passed" enumeration on two identical *failure* bodies (T5's honest transport-failure copy) → acceptance is now asserted before equality; (2) A8 moved to the REAL auth client (`sign_in_with_oauth` performs no network call, `code_verifier=""` → storage fallback), so PKCE is LIVE evidence at zero SMTP cost instead of me asserting against my own fake; (3) "verifier cleared after redemption" was theatre under a fake (library-side `remove_item`) → replaced by exchange-arg-shape + replay-under-cleared-bucket claims, with §8.1's six clearing sites cited as STATIC from t10 CHECK 2. Added A5b: leading-zero codes (`07368987`) must reach GoTrue verbatim as `str` (any `int()` truncates to 7 digits and silently locks real users out) while 7/9-digit, `abcdefgh`, `²0245678`, `١٢٣٤٥٦٧٨`, `"07368 987"` never reach it — the Unicode tiers are why `[0-9]` beats `\d`. NOT FIRED: awaiting t6/t8 green + the captain's go, and the outstanding address-space ruling (T1 §6: GoTrue rejects reserved `@example.com`, so A2's two live slots + P0 mint need `--domain sprintspike-otp.dev`).
[uat-validator] 2026-09-16 11:40 TWO CORRECTIONS to my 11:15 line, both mine. (1) "zero live writes / ledger untouched" was WRONG: an explicit `--only 4 --selftest` ran a live mint because my write-ban guarded only the fan-out path, so `uat-otp-a5@example.com` now exists in auth.users (id 9c2714bf-aa18-4ab6-a2e9-ee2306560cee, read back via admin.list_users) — 1 of the 8 user caps spent, 0 SMTP slots spent, no user_profiles row created, address recorded in the ledger + plan as a purge-list entry, nothing deleted by me. Now blocked by construction: `_no_live_writes()` hard-refuses steps 1–4 under `--selftest` on every invocation path (verified by firing it). Re-ran the full self-test after the fix: 70 assertions, 0 fail, 3 named gaps, ledger unchanged → the write-free claim holds from here on. (2) The address-space "blocker" I escalated was over-stated: T1 §6 measured the PUBLIC `sign_in_with_otp` path, while the ADMIN `generate_link` path accepts `@example.com` — proven by that very user existing. So P0/A4/A5/A6/A10/A12 run in the sanctioned space at zero SMTP cost; only A2's new-address slot is exposed, and the acceptance guard reports enumeration as NOT-VERIFIED rather than falsely passing. `--domain sprintspike-otp.dev` is now a scope choice for the captain, not a schedule dependency. Still NOT FIRED; port 5000 free; nothing else touched.
[uat-validator] 2026-09-16 12:35 husk-deletion notice received and acted on. Audited my whole surface: `uat/` and `tests/` contain **no** reference to `11c3e099-…`/`cdb275ae-…`/`uat.user.1788795034`/`live_qa_1788762183`, so my A12 baseline (`behave-full-run1.log`: 210 scenarios / 58 fail / 30m38s, recorded on the intact seed set) is NOT invalidated and nothing needs regenerating beyond creating fresh `uat-otp-*` identities at run time, which the plan already says. Made the create-only rule executed rather than intended: step 0 now runs `audit_myself()`, which parses my own scripts with **ast** and fails the run on any delete/delete_user/drop/truncate/purge *call* (docstring prose can't false-flag it — my first draft flagged itself on the words and I rewrote it), checks `tests/`+`templates/` for live dependencies on the deleted accounts, and prints cap headroom BEFORE any write. Headroom: 1 spent (`uat-otp-a5@example.com`, my own breach above) + 5 to mint (p0, a2new, a2exist, a4, a6) = **6/8**, 2 spare for a post-fix re-run; if a fix forces extra mints I stop and ask instead of exceeding the cap. I still delete nothing, not even my own leftovers — the incident is exactly why the report carries the purge list for the user. Re-ran the full write-free self-test on `e89e740`: **74 assertions, 0 fail, 3 named gaps** (25 LIVE / 23 FAKE-TRANSPORT / 26 STATIC), 1m38s; ledger unchanged, port 5000 free, still not fired.
[qa] 2026-09-16 13:55 t4 baseline at 6447d58 (suites run sequentially — shared live Supabase): full pytest 204 pass / 1 fail / 15 errors in 23.7s; auth-targeted pytest (test_auth_otp + test_config_guards + test_csrf) 43/43 green; behave auth-hardening 7/7 scenarios, 34/34 steps green (7.1s). Triage: the 1 fail = test_mentor_grounding term-extractor drift (deterministic, no LLM, file last touched 29ac797 which is an ancestor of T1 → pre-existing, non-auth); the 15 errors = tests/run_comprehensive_visual_test.py `fixture 'page' not found` (pytest-playwright not installed; env artifact, also why no --timeout flag). Effective auth regressions: 0. Report: uat/reports/qa-baseline-t4.md.
[qa] 2026-09-16 14:10 t5 LIVE localhost OTP round-trip via admin generate_link, on 6447d58 (server 127.0.0.1:5000, real HTTP + cookie jar, live Supabase; 0 SMTP slots spent, POST /auth/otp/verify never touched /otp/send): pinned type="email" redeems BOTH families — signup-family code for first-ever uat-otp-p0@sprintspike-otp.dev (302→/sprints 200, exactly 1 user_profiles row, created_by_this_call=True) and magiclink-family code for existing uat-otp-p0legacy@... (same shape, no duplicate row); replays rejected (status 200 not 302 = no session; probe confirms generic copy + /sprints→302 login). Raw evidence JSON shows 2 replay FAILs — probe-side HTML-escaping match bug (didn&#39;t), re-verified by 0-cost probe; script fixed. Ledger now 3/8 users (a5 residue + p0 + p0legacy, names match planned P0 so the gated window reuses at zero cost), sends 0/2 untouched, port 5000 released, no deletes. Report: uat/reports/qa-t5-roundtrip.md. ⚠️ ESCALATION: uat/scripts/run_t7_uat.py is 188 MB of repeated step-1 text (corrupted ~12:51, mtime frozen); the STATUS self-test claims can't refer to this artifact — driver needs rebuild before the full window; untouched by me.



