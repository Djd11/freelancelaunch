{
  "name": "sprint-platform-lesson-player",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx esbuild src/index.tsx --bundle --outfile=../static/video/lesson-player.js --format=iife --platform=browser --jsx=automatic --define:process.env.NODE_ENV='\"production\"' --minify --sourcemap"
  },
  "dependencies": {
    "@remotion/player": "4.0.484",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "remotion": "4.0.484"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
